# Container
docker container ls -a

# Remove all container
docker rm -v -f $(docker ps -qa)

sudo chmod -R 400 GLMs.pem

 ssh -i /Users/victor/Downloads/GLMs.pem ubuntu@ec2-18-202-222-188.eu-west-1.compute.amazonaws.com
